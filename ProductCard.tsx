export default function ProductCard({product}:any){return <article className="card"><div className="image">{product.tag||"TB"}</div><h3>{product.name}</h3><p className="muted">{product.brand} • {product.category}</p><p>{product.desc}</p><p className="price">{Number(product.price)>0?`${product.price} NOK`:"Pris settes senere"}</p><p className="muted">Lager: {product.stock||0}</p><button className="btn">Legg i kurv</button></article>}